
	<% cart.each do |p| %>
		<p><%= p %></p>
	<% end %>

<%= form_tag("/products", method: "post") do %>
  <%= label_tag(:q, "Product:") %>
  <%= text_field_tag(:product) %>
  <%= submit_tag("add to cart") %>
<% end %>